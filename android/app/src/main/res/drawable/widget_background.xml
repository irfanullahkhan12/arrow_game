<?xml version="1.0" encoding="utf-8"?>
<!-- The widget card: the same deep rose-plum the board uses in the app, so a
     neon arrow reads as neon out on the home screen too. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="26dp" />
    <gradient
        android:angle="270"
        android:startColor="#4E1140"
        android:endColor="#2C0722" />
    <stroke
        android:width="2dp"
        android:color="#FF5CC8" />
</shape>
